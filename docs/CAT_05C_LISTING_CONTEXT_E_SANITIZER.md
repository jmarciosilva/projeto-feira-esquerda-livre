# CAT-05C — ponteiro

> **Este arquivo não contém mais documentação.** Ele existe apenas porque dois
> docblocks de código ainda o citam (`app/CatalogIntelligence/Support/ContextSanitizer.php`
> e `app/CatalogIntelligence/DTOs/ListingContext.php`), e a consolidação
> documental DOC-CONSOLIDATION-01 não altera código.

O conteúdo da CAT-05C foi consolidado em:

- **Dívida C-1** (`knownAttributes` protegido por lista de proibição; a CAT-09
  deve mapear campo a campo) e **dívida C-2** (redação de texto livre) —
  [`docs/ARCHITECTURE.md`](ARCHITECTURE.md), seção *Catalog Intelligence*, e
  [`ROADMAP.md`](../ROADMAP.md), seção *Dívidas técnicas*.
- **Decisões D-CAT-05C-1 a D-CAT-05C-7** — Decision Log de
  [`docs/ARCHITECTURE.md`](ARCHITECTURE.md).
- **Estado da subfase** — [`ROADMAP.md`](../ROADMAP.md), seção *Catalog Intelligence*.

O texto original permanece no histórico do Git (último commit com o documento
completo: `6ae5981`).

Quando os dois docblocks forem atualizados para apontar para
`docs/ARCHITECTURE.md`, este arquivo pode ser removido.
